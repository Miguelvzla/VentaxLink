# Coincide con infra/docker-compose.yml (usuario/clave/db ventaxlink).
# Copiá este archivo a `.env` en esta misma carpeta (`packages/database/.env`).
DATABASE_URL=postgresql://ventaxlink:ventaxlink@localhost:5432/ventaxlink
